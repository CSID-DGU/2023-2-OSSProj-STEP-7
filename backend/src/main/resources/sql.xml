<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.backend.mapper.UserMapper">
    <!-- 로그인 -->
    <select id="getUserAccount" resultType="com.example.backend.vo.UserVo">
        SELECT * FROM TB_USER
        WHERE USER_ID= #{userId}
    </select>

    <!-- 회원가입 -->
    <insert id="saveUser" parameterType="com.example.backend.vo.UserVo">
        INSERT INTO TB_USER
            (USER_ID, USER_PW, USER_NAME)
        VALUES(#{userId},#{userPw},#{userName});
    </insert>
</mapper>